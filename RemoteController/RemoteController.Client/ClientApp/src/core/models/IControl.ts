export interface IControl {
  /**
   * Unique name of control. Note: this is reserved property by ControlsService.
   *
   * @type {string}
   * @memberof IControl
   */
  name: string;
  col: number;
}
