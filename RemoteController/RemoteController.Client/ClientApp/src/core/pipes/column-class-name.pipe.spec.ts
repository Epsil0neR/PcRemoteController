import { ColumnClassNamePipe } from './column-class-name.pipe';

describe('ColumnClassNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ColumnClassNamePipe();
    expect(pipe).toBeTruthy();
  });
});
