<Window
    x:Class="RemoteController.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:pages="using:RemoteController.ViewModels.Pages"
    mc:Ignorable="d">

    <NavigationView
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch"
        ItemInvoked="NavigationView_OnItemInvoked"
        MenuItemsSource="{x:Bind ViewModel.NavigationItems}"
        SelectedItem="{x:Bind ViewModel.FirstPage}"
        >
        <NavigationView.MenuItemTemplate>
            <DataTemplate x:DataType="pages:IPageViewModel">
                <NavigationViewItem 
                    Content="{x:Bind Title}" 
                    DataContext="{x:Bind}">
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="{x:Bind Symbol}" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </DataTemplate>
        </NavigationView.MenuItemTemplate>
        <Frame 
            x:Name="Frame"
            VerticalAlignment="Stretch"
            HorizontalAlignment="Stretch"
            VerticalContentAlignment="Stretch"
            HorizontalContentAlignment="Stretch"
            Loaded="Frame_OnLoaded"
            />
    </NavigationView>
</Window>
